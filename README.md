This was created as an interpreter for MIPS Assembly for CDA 3012 Computer Architecture. We didn't have an interpreter, so I ended up making this in a couple of weeks. Then we got one from the class, so, this now exists.

Most of the basic instructions work, the general purpose registers exist. Anything related to floating point numbers was not added as that wasn't covered in class.
